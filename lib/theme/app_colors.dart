import 'package:flutter/material.dart';

class Appcolors {
  // Light Theme Colors
  static const Color primaryLight = Colors.black;
  static const Color backgroundLight = Colors.white;
  static const Color accentLight = Colors.black; // for buttons as well
  static const Color textLight = Colors.black87;
  static const Color containerLight = Colors.white;

  // Dark Theme Colors
  static const Color primaryDark = Colors.white;
  static const Color backgroundDark = Color(0xFF121212);
  static const Color accentDark = Colors.white70; // for buttons as well
  static const Color textDark = Colors.white70;
  static const Color containerDark = Color.fromRGBO(2, 2, 49, 1.0);

  // Shared Colors
  static const Color dividerColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color successColor = Colors.greenAccent;
}
